# ttl-cache-js
Cache module with TTL option for Node JS


Import:
    var Cache = require('ttl-cache-js');
    
Instantiate:
    
    var cache = Cache();
    
    // or:
    var cache = new Cache();
    
    
Api:
    // get:
    // set:
    // del:
    // clear: 
    // size: